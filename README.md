# PWA Glossary API

FastAPI приложение для управления глоссарием терминов Progressive Web Apps (PWA) с поддержкой семантического графа и интерактивной визуализации.

## 📋 Отчет о проделанной работе

### Реализованный функционал

1. **Backend API (FastAPI)**
   - ✅ Полный CRUD для терминов глоссария
   - ✅ RESTful API с валидацией через Pydantic v2
   - ✅ Эндпоинт для получения семантического графа
   - ✅ In-memory хранилище с предустановленными PWA терминами
   - ✅ Автоматическая документация API (Swagger/ReDoc)

2. **Frontend (Vanilla JavaScript)**
   - ✅ Интерактивный интерфейс с двумя представлениями:
     - **Глоссарий**: карточки терминов, поиск, фильтрация, CRUD операции
     - **Семантический граф**: интерактивная визуализация связей между терминами
   - ✅ Визуализация графа с помощью vis.js
   - ✅ Модальные окна для добавления/редактирования терминов
   - ✅ Адаптивный дизайн

3. **Семантический граф**
   - ✅ Автоматическое построение связей между терминами
   - ✅ Цветовая кодировка по категориям
   - ✅ Интерактивная навигация (клик по узлам, масштабирование)
   - ✅ Визуализация направленных связей со стрелками

4. **Docker и деплой**
   - ✅ Оптимизированный Dockerfile для продакшн
   - ✅ Многоэтапная сборка для уменьшения размера образа
   - ✅ Безопасная конфигурация (непривилегированный пользователь)

### Технические детали

- **Валидация**: Pydantic v2 с поддержкой алиасов для сериализации
- **Архитектура**: Модульная структура (models, database, main)
- **Безопасность**: Валидация всех входных данных, защита от инъекций
- **Производительность**: Оптимизированная сериализация JSON

## 🚀 Быстрый старт

### Локальная разработка

```bash
# Установка зависимостей
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Запуск
uvicorn main:app --reload
```

Приложение будет доступно по адресу: **http://localhost:8000**

### Docker деплой

```bash
# Сборка образа
docker build -t pwa-glossary .

# Запуск контейнера
docker run -d -p 8000:8000 --name pwa-glossary pwa-glossary

# Или с docker-compose
docker-compose up -d
```

## 📁 Структура проекта

```
shakh/
├── main.py              # FastAPI приложение и endpoints
├── models.py            # Pydantic модели для валидации
├── database.py          # In-memory хранилище данных
├── requirements.txt     # Зависимости Python
├── Dockerfile           # Конфигурация Docker
├── .dockerignore        # Исключения для Docker
├── .gitignore          # Исключения для Git
├── README.md           # Документация
└── static/             # Frontend файлы
    ├── index.html      # Главная страница
    ├── app.js          # JavaScript логика
    └── styles.css      # Стили
```

## 🔌 API Endpoints

### Термины

- `GET /api/terms` - Получить список всех терминов
- `GET /api/terms/{keyword}` - Получить информацию о конкретном термине
- `POST /api/terms` - Добавить новый термин
- `PUT /api/terms/{keyword}` - Обновить существующий термин
- `DELETE /api/terms/{keyword}` - Удалить термин

### Граф

- `GET /api/graph` - Получить данные семантического графа (узлы и связи)

### Система

- `GET /api/health` - Проверка работоспособности API

### Документация

- `GET /docs` - Swagger UI
- `GET /redoc` - ReDoc

## 💻 Примеры использования API

### Создание термина

```bash
curl -X POST "http://localhost:8000/api/terms" \
  -H "Content-Type: application/json" \
  -d '{
    "keyword": "pwa",
    "title": "Progressive Web Application (PWA)",
    "definition": "Прогрессивное веб-приложение — это веб-приложение, использующее современные веб-технологии для обеспечения опыта, сопоставимого с нативными мобильными приложениями.",
    "source": "https://web.dev/progressive-web-apps/",
    "category": "Концепция",
    "related_terms": ["service-worker", "offline-first"]
  }'
```

### Получение графа

```bash
curl "http://localhost:8000/api/graph"
```

## 🎨 Frontend возможности

### Глоссарий
- Просмотр всех терминов в виде карточек
- Поиск по названию и ключевому слову
- Фильтрация по категориям
- Добавление, редактирование и удаление терминов
- Просмотр детальной информации о термине

### Семантический граф
- Интерактивная визуализация связей между терминами
- Цветовая кодировка по категориям
- Клик по узлу для просмотра деталей термина
- Масштабирование и перетаскивание графа
- Направленные связи со стрелками

## 📦 Предустановленные термины

Приложение поставляется с предустановленными PWA терминами, такими как:
- Progressive Web Application (PWA), Service Worker, Web App Manifest
- Offline First, Cache API, Background Sync
- Push-уведомления, Notifications API, Installable
- Display Mode, Lighthouse, Производительность, Адаптивный дизайн, HTTPS, App Shell Model

## 🛠 Технологии

- **Backend**: FastAPI, Pydantic v2, Python 3.11+
- **Frontend**: Vanilla JavaScript, vis.js для графа
- **Хранилище**: In-memory (можно заменить на PostgreSQL/MongoDB)
- **Контейнеризация**: Docker с многоэтапной сборкой

## 📝 Примечания

- Данные хранятся в памяти и сбрасываются при перезапуске сервера
- Для продакшена рекомендуется использовать реальную базу данных
- Все поля терминов валидируются через Pydantic схемы
- Docker образ оптимизирован для продакшн использования

## 🔧 Разработка

Для локальной разработки с hot-reload:

```bash
./run.sh
# или
uvicorn main:app --reload
```

## 📄 Лицензия

Проект создан в образовательных целях.
